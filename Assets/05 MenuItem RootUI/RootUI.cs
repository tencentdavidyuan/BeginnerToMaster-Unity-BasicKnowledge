using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace BeginnerToMaster.Exmaple {

    public class RootUI : MonoBehaviour {
        public RectTransform _headUpCanvas;
        public RectTransform _operationCanvas;
        public RectTransform _normalCanvas;
        public RectTransform _modalCanvas;
        public RectTransform _tipsCanvas;
        public RectTransform _systemCanvas;
        public RectTransform _joyStickCanvas;
        public RectTransform _newGuideCanvas;


        [SerializeField]
        private Canvas _topCanvas;

        // Start is called before the first frame update
        void Start() {

        }

        // Update is called once per frame
        void Update() {

        }
    }
}